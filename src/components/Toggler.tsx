const Toggler = (toggleTheme:any) => {
    function toggleEvent() {

    }

    return (
        <button onClick={toggleEvent}> 
          button
        </button>
    );
}

export default Toggler;

// export {};